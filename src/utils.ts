// export const getSiblings = (element: any) => {
//   let siblings = [];
//   let sibling = element.parentNode.firstElementChild;

//   while(sibling) {
//       if (sibling.nodeType === 1 && sibling !== element) {
//           siblings.push(sibling);
//           sibling = sibling.nextElementSibling;
//       }
//   }

//   return siblings;
// }